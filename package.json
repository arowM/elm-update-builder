{
  "private": true,
  "scripts": {
    "format": "elm-format src",
    "start": "elm-doc-preview",
    "review": "elm-review --fix",
    "test": "elm-review && elm-format --validate src && elm make --docs=/tmp/docs.json && elm-verify-examples --fail-on-warn -r"
  },
  "devDependencies": {
    "elm-doc-preview": "^5.0.3",
    "elm-format": "^0.8.3",
    "elm-test": "^0.19.1",
    "elm-verify-examples": "^5.0.0"
  },
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-review": "^2.5.1"
  }
}
